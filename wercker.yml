box: debian
build:
    steps:
        - arjen/hugo-build:
            theme: hugo-theme-cactus-plus
            flags: --buildDrafts=true
deploy:
    steps:
        - install-packages:
            packages: git ssh-client
        - leipert/git-push:
            token: $GIT_TOKEN
            domain: seanzhang0316.github.io
            basedir: public

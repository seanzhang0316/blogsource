box: debian
build:
    steps:
        - arjen/hugo-build:
            theme: hyde
            flags: --buildDrafts=true
deploy:
    steps:
        - install-packages:
            packages: git
        - leipert/git-push:
            gh_oauth: $GIT_TOKEN
            repo: seanzhang0316/seanzhang0316.github.io
            branch: master
            basedir: public
